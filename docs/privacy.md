# 本地隐私与数据边界

Prompt Hub 的核心提示词库使用本机 SQLite 数据库。备份也保存在本机数据目录。导入文件仅在本地解析；MCP 通过显式配置的本地数据库路径访问数据。

诊断界面不显示提示词正文、密钥或授权头。当前核心工作流不要求 AI 提供商凭据。若未来配置 AI 提供商，凭据必须只使用操作系统凭据存储，不能写入数据库、日志、测试夹具或仓库文件。

外部来源、AI 与 MCP 的写入必须先进入收件箱，不能覆盖已发布提示词。
